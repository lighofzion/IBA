{
    "buildCommand": "node build.js",
    "outputDirectory": "dist",
    "builds": [
      {
        "src": "dist/**",
        "use": "@vercel/static"
      }
    ],
    "scripts": {
      "build": "node build.js"
    }
  }