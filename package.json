{
    "name": "iba",
    "version": "1.0.0",
    "buildCommand": "node build.js",
    "outputDirectory": "dist",
    "builds": [
      {
        "src": "dist/**",
        "use": "@vercel/static"
      }
    ],
    "scripts": {
      "build": "node build.js"
    },
    "devDependencies": {
      "@vercel/static": "^1.0.0"
    }
  }